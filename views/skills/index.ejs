<body>
    <h1>Skills</h1>
    <ul>
        <% skills.forEach(function(t) { %>
        <li>
            <%= t.skill %>
            - 
            <%= t.done ? 'have' : 'dont have' %>
        </li>
        <% }); %>
    </ul>
</body>